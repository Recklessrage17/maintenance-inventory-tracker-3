# Website mode: React talks to the backend API instead of browser IndexedDB.
VITE_MIT3_DATA_SOURCE=api

# Use this during frontend dev when the backend is running separately.
# Leave blank for production when the backend serves frontend/dist from the same origin.
VITE_MIT3_API_BASE_URL=http://localhost:4173
